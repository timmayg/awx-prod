---
 - hosts: localhost
   tasks:
     - set_fact:
         time1: "{{ ansible_date_time }}"

     - set_fact:
         time2: 2024-08-17T02:00:00Z

     - debug:
         msg: "{{ time1 }}"
         msg: "{{ time2 }}"